Model:
QWZ
$$H(\vec{k}) = d_x(\vec{k}) + d_y(\vec{k}) + d_z(\vec{k})$$
where 
$ d_x = A\sin{k_x}\sigma_x, \ d_y = A\sin{\vec{k}\sigma_y}, \ d_z = (m+B(2-\cos{k_x}-\cos{k_y}))\sigma_z$.

I am looking at the effect of changing the embedding, shifting one orbital species relative to the other in real space, creatign sublattices. I expect there. will be am effect on the Berry flux and curvature distribution in k-space, and this will mean the Chern number of a pertially filled band can differ under embedding changes, but that the total chern nubmer fo the band must remain the same.

Consider embedding in general as 
$$U(\vec{k}) = e^{-i\theta(\vec{k})\sigma_z} = ((e^{-i\theta(\vec{k})}, 0); 0, e^{i\theta(\vec{k})})$$
